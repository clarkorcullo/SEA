╔══════════════════════════════════════════════════════════════════════════════╗
║          LOGIN SECURITY SEQUENCE DIAGRAM - VISUAL REPRESENTATION             ║
║          Social Engineering Awareness Program                                ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌──────────┐
│   USER   │
│ (Browser)│
└────┬─────┘
     │
     │ 1. Enter username & password
     │
     ▼
┌─────────────────┐
│   FRONTEND      │
│  (Login Form)   │
└────┬────────────┘
     │
     │ 2. POST /login (HTTPS encrypted)
     │
     ▼
┌─────────────────┐
│  LOGIN ROUTE    │
│   (/login)      │
└────┬────────────┘
     │
     │ 3. Input Validation & Sanitization
     │    ✓ Format checks
     │    ✓ Length validation
     │    ✓ SQL injection prevention
     │
     ▼
┌─────────────────┐
│  USER SERVICE   │
│ authenticate()  │
└────┬────────────┘
     │
     │ 4. get_by_username(username)
     │
     ▼
┌─────────────────┐
│   USER MODEL    │
└────┬────────────┘
     │
     │ 5. Database Query (Parameterized)
     │    SELECT * FROM user WHERE username=?
     │
     ▼
┌─────────────────┐
│    DATABASE     │
│   (PostgreSQL)  │
└────┬────────────┘
     │
     │ 6. Return user record (if exists)
     │
     ▼
┌─────────────────┐
│   USER MODEL    │
└────┬────────────┘
     │
     │ 7. check_password(plain_password)
     │
     ▼
┌─────────────────────────────────────────┐
│      PASSWORD VERIFICATION              │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │ PBKDF2 Hash Verification        │  │
│  │                                  │  │
│  │ 1. Extract salt from hash       │  │
│  │ 2. Hash password with salt      │  │
│  │ 3. Constant-time comparison     │  │
│  │ 4. Return True/False             │  │
│  └──────────────────────────────────┘  │
│                                        │
│  Security Features:                    │
│  ✓ Unique salt per password           │
│  ✓ Multiple iterations                │
│  ✓ Timing attack protection           │
│  ✓ No plaintext storage               │
└────┬───────────────────────────────────┘
     │
     │
     ├─────────────────────────────────────┐
     │                                     │
     │  PASSWORD INCORRECT                 │
     │  ┌──────────────────────────────┐   │
     │  │ 8a. Log failed attempt       │   │
     │  │ 8b. Generic error message    │   │
     │  │ 8c. Return to login form     │   │
     │  └──────────────────────────────┘   │
     │                                     │
     └─────────────────────────────────────┘
     │
     │  PASSWORD CORRECT
     │
     ▼
┌─────────────────────────────────────────┐
│      SESSION CREATION                   │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │ login_user(user)                 │  │
│  │                                  │  │
│  │ Secure Cookie Configuration:     │  │
│  │ ✓ HttpOnly: True                 │  │
│  │ ✓ Secure: True (HTTPS)           │  │
│  │ ✓ SameSite: Lax/None             │  │
│  │ ✓ Expiration: 24 hours           │  │
│  │ ✓ Session ID: Random             │  │
│  └──────────────────────────────────┘  │
│                                        │
│  Security Benefits:                    │
│  ✓ XSS protection (HttpOnly)          │
│  ✓ MITM protection (Secure)           │
│  ✓ CSRF protection (SameSite)         │
│  ✓ Auto-expiration                     │
└────┬───────────────────────────────────┘
     │
     │ 9. Log successful login
     │
     ▼
┌─────────────────┐
│ SECURITY LOGGER │
└────┬────────────┘
     │
     │ 10. Check profile completion
     │
     ▼
┌─────────────────┐
│    REDIRECT     │
└────┬────────────┘
     │
     ├──────────────────────┐
     │                      │
     ▼                      ▼
┌─────────────┐    ┌──────────────┐
│   PROFILE   │    │  DASHBOARD   │
│  (if needed)│    │  (complete)  │
└─────────────┘    └──────────────┘

═══════════════════════════════════════════════════════════════════════════════

SECURITY LAYERS IMPLEMENTED:

┌─────────────────────────────────────────────────────────────────────────┐
│ LAYER 1: INPUT SECURITY                                                 │
│  • HTTPS encryption (data in transit)                                    │
│  • Input sanitization                                                    │
│  • Format validation                                                     │
│  • SQL injection prevention (ORM)                                       │
└─────────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ LAYER 2: AUTHENTICATION SECURITY                                         │
│  • Parameterized database queries                                        │
│  • PBKDF2 password hashing                                               │
│  • Unique salt per password                                              │
│  • Constant-time comparison                                              │
│  • Generic error messages (no enumeration)                               │
└─────────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ LAYER 3: SESSION SECURITY                                                │
│  • HttpOnly cookies (XSS protection)                                     │
│  • Secure flag (HTTPS only)                                              │
│  • SameSite protection (CSRF protection)                                 │
│  • Automatic expiration (24 hours)                                        │
│  • Cryptographically random session IDs                                  │
└─────────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ LAYER 4: MONITORING & LOGGING                                            │
│  • Success login logging                                                 │
│  • Failed attempt logging                                                │
│  • Security event tracking                                               │
│  • Audit trail maintenance                                               │
└─────────────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════════

SECURITY FEATURES SUMMARY:

✅ Password Security
   • PBKDF2 hashing algorithm
   • Unique salt per password
   • No plaintext storage
   • Constant-time verification

✅ Session Security
   • HttpOnly flag (XSS protection)
   • Secure flag (HTTPS only)
   • SameSite flag (CSRF protection)
   • 24-hour expiration

✅ Input Security
   • SQL injection prevention (ORM)
   • XSS prevention (auto-escaping)
   • Input validation
   • Generic error messages

✅ Logging & Monitoring
   • Comprehensive audit trail
   • Failed attempt tracking
   • Security event logging

═══════════════════════════════════════════════════════════════════════════════


